# Default: free, deterministic, offline-safe demo provider.
DYSTRONIC_AI_PROVIDER=demo

# Optional provider. Never expose this value with a NEXT_PUBLIC_ prefix.
# DYSTRONIC_AI_PROVIDER=openai
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-5.6-sol
